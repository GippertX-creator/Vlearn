# Vlearn 系统第四次更新提示词（增量开发，基于第三版已实现功能）

> 当前系统已实现：三角色物理隔离（教务/财务/助教独立数据库）、学生个性化费用、老师课酬差异化、退费/转课/暂停、操作日志、报表优化、教务/财务 Agent、助教模块（含大模型短信生成）、定时备份（本次新增前暂无自动备份）。  
> **本次更新目标**：  
> 1. 新增财务可视化图表页面（经营分析）。  
> 2. 新增“老师简报”功能，由财务生成报告，供转发给任课老师（老师本人不登录系统）。  
> 3. 增加教室资源管理，支持多校区，教室参与排课冲突检测与利用率统计。  
> 4. 增加定时备份功能，每周自动备份数据库，启动时补备份，所有角色可配置。  
> **不涉及**：智能排课优化（本次不更新）。  

---

## 1. 财务可视化图表（经营分析页面）

### 1.1 新增页面
- 在财务角色菜单中新增“经营分析”页面，位于“仪表盘”之后。
- 该页面仅财务角色可见，教务、助教完全不可见（前端不渲染，后端 IPC 校验角色）。

### 1.2 图表内容
页面包含以下三张图表（使用 `@ant-design/charts` 实现，与 Ant Design 风格一致）：

1. **月度收支盈亏趋势图**（折线图或柱状图）
   - 展示月度总收入、总支出、净盈亏，支持按月/季度/年度切换。
   - 数据来源：财务数据库中的学生缴费（收入）与老师课酬（支出）。

2. **收入构成饼图**
   - 展示收入按课程类型/科目分布，或按缴费方式（微信、转账、现金等）分布，可由用户切换。
   - 数据来源：学生缴费记录关联教务课程信息（通过只读 IPC 获取课程名称）。

3. **支出构成饼图**
   - 展示支出按老师或按课程分布，可由用户切换。
   - 数据来源：老师课酬支付记录关联教务老师/课程信息。

### 1.3 时间维度与筛选
- 支持按月、季度、年度快速切换，以及自定义日期范围。
- 所有图表联动筛选：切换时间范围后，三张图表同时更新。

### 1.4 交互与下钻
- 图表支持点击下钻：例如点击某月柱子，下方显示该月详细收支列表（学生缴费明细、老师课酬明细），表格可导出。

### 1.5 技术实现
- 前端使用 `@ant-design/charts`（或 `@ant-design/plots`）绘制图表。
- 图表数据通过 IPC 从主进程查询，主进程仅允许财务角色调用相关接口。
- 导出图表为图片功能暂不要求，可选实现。

---

## 2. 老师简报功能

### 2.1 功能定位
- 由**财务角色**在财务端生成，用于转发给任课老师，老师本人不登录系统。
- 简报内容不包含任何财务隐私数据（课酬是否包含由财务在生成时选择，默认不包含）。

### 2.2 生成入口
- 在财务角色新增“老师简报”页面。
- 页面列出所有老师，财务选择一个老师后，可配置简报内容并生成。

### 2.3 简报内容选项
每次生成前，财务可勾选要包含的部分：
- **基本信息**：老师姓名、所教课程列表。
- **课程安排表**：未来 7 天或指定时间范围内的课程实例（日期、时间、课程名、班级号、教室）。
- **学生名单及出勤情况**：该老师所教课程对应的学生名单，以及近期（如最近 4 次课）出勤统计（出勤/请假/缺勤次数）。
- **课酬汇总**（可选，默认不勾选）：该时间段内老师的应付课酬、实付课酬。**若勾选，需二次确认，且不可包含学生缴费等财务信息。**

### 2.4 报告展示与导出
- 生成后在弹窗中预览，提供“打印/导出 PDF”按钮（可使用 Electron 的 `webContents.printToPDF` 或调用系统打印）。
- 同时提供“复制为文本”功能，便于财务粘贴到微信/邮件发送。

### 2.5 数据来源与权限
- 老师简报所需数据（老师信息、课程安排、学生出勤）来自教务数据库，财务角色通过只读 IPC 获取。
- 课酬数据来自财务数据库。
- 所有 IPC 接口需校验角色为财务，且数据只读，不提供写入。

### 2.6 与现有角色的关系
- 不新增“老师”角色，老师不登录系统，仅由财务生成简报后线下转发。
- 助教角色不变，不涉及此功能。

---

## 3. 教室资源管理（多校区支持）

### 3.1 新增校区管理
- 新增“校区”概念，存储在教务数据库（`vlearn_academic.db`）中。
- 校区字段：`id`、`name`（如“本部”“东校区”）、`address`、`note`。
- 由教务角色进行增删改查，财务和助教只读。

### 3.2 新增教室表
在教务数据库中新增 `classrooms` 表：
```sql
CREATE TABLE classrooms (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  campus_id INTEGER NOT NULL,
  name TEXT NOT NULL,
  capacity INTEGER,
  type TEXT NOT NULL DEFAULT '普通',        -- 普通/多媒体/实验室等
  note TEXT,
  device_info TEXT,                        -- 设备信息
  status TEXT DEFAULT 'available',         -- available / maintenance / disabled
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now')),
  FOREIGN KEY (campus_id) REFERENCES campuses(id) ON DELETE CASCADE
);
```

### 3.3 课程与教室关联
- 在 `courses` 表中新增字段：
  ```sql
  ALTER TABLE courses ADD COLUMN default_classroom_id INTEGER;
  ```
  表示该课程的默认教室（可空），由教务设置。
- 在 `schedule_instances` 表中新增字段：
  ```sql
  ALTER TABLE schedule_instances ADD COLUMN classroom_id INTEGER;
  ```
  表示该课程实例实际使用的教室。若为 NULL，则继承课程的默认教室。支持单独修改（调课时可更换教室）。

### 3.4 教室冲突检测
- 排课冲突检测规则扩展为三类：
  1. 老师时间冲突
  2. 学生时间冲突
  3. **教室时间冲突**（同一时间同一教室不能安排两个课程）
- 在新增课程或调整课程实例时，实时检测以上冲突并弹出提醒（现有 Agent 冲突检测逻辑需更新）。
- 冲突检测需考虑 `classroom_id` 和 `campus_id`（不同校区的教室不冲突，但同一校区内教室不能冲突）。

### 3.5 教室利用率统计
- 新增“教室利用率”页面，位于教务角色菜单下。
- 展示每个校区、每间教室在指定时间范围内的使用率（已排课时间 / 可用时间），可查看空闲时段。
- 帮助教务优化排课，避免教室闲置或过度使用。

### 3.6 教室管理界面
- 教务角色新增“校区管理”和“教室管理”页面（或合并为一个“资源管理”页面）。
- 教务可增删改校区和教室，设置状态、设备信息等。
- 财务和助教通过只读接口查看教室信息，用于排课展示和简报。

### 3.7 多校区注意事项
- 所有数据模型和界面需支持多校区，例如课程和教室需关联校区，排课冲突检测需考虑校区。
- 学生和老师不强制关联校区，可跨校区授课，冲突检测基于具体时间与教室。

---

## 4. 定时备份功能

### 4.1 备份内容
- 仅备份三个数据库文件（`vlearn_academic.db`、`vlearn_finance.db`、`vlearn_assistant.db`）。
- 不备份配置文件（如 API key）和日志文件。

### 4.2 备份频率与触发
- 每周自动备份一次，默认每周日凌晨 3:00。
- 备份时间可在系统设置中配置（所有角色共享该设置）。
- **应用启动时检查**：如果上次备份时间超过设定周期（例如超过一周），则启动后立即执行一次备份，防止错过。

### 4.3 备份位置与保留策略
- 备份保存到用户指定的本地目录（初始默认为系统用户目录下的 `VlearnBackups` 文件夹）。
- 保留最近更新的备份文件，只保留最近 4 份（可配置），自动删除更旧的备份。

### 4.4 用户配置入口
- 在“系统设置”页面新增“备份设置”区域，所有角色均可配置（教务、财务、助教）。
- 可设置：备份目录路径、备份周期（每周几、时间）、保留份数。
- 保存设置后立即生效，并显示下次备份时间。

### 4.5 备份通知
- 备份成功或失败时，在主界面侧边栏显示系统通知（非 Agent 通知，是系统级通知）。
- 失败时提供错误信息，方便用户排查（如目录不可写、磁盘空间不足）。

### 4.6 恢复功能（本次不提供界面）
- 暂不提供恢复界面，用户可手动将备份文件复制回数据库目录进行恢复（需在 README 中说明）。
- 未来可考虑增加恢复向导。

### 4.7 技术实现
- 在主进程中使用 `node-cron` 或 `setInterval` 定时任务，每周触发备份。
- 备份操作：将三个数据库文件复制到目标目录，文件名格式：`vlearn_academic_YYYYMMDD_HHmmss.db` 等。
- 启动时检查上次备份时间：可在 `settings` 表中记录 `last_backup_time`，与当前时间比较决定是否补备份。
- 所有备份操作异步执行，不阻塞主界面。

---

## 5. 数据迁移与兼容性

- 本次涉及教务数据库新增表和字段（`campuses`、`classrooms`、`courses.default_classroom_id`、`schedule_instances.classroom_id`）。
- 财务数据库和助教数据库无结构变更。
- 迁移脚本自动执行，确保旧数据不受影响，新字段有默认值。
- 备份设置存储在各自数据库的 `settings` 表中，三库需保持一致（可统一由主进程读取，或每个角色设置时写入当前角色库并同步到其他库）。

---

## 6. 权限与 IPC 调整

| 功能模块 | 教务 | 财务 | 助教 |
|----------|------|------|------|
| 校区管理 | 读/写 | 读 | 读 |
| 教室管理 | 读/写 | 读 | 读 |
| 教室利用率统计 | 查看 | 读 | 读 |
| 老师简报生成 | 不可见 | 使用 | 不可见 |
| 经营分析页面 | 不可见 | 使用 | 不可见 |
| 备份设置 | 配置 | 配置 | 配置 |

- 所有新增 IPC 接口在主进程校验角色，如财务访问教务数据（教室、课程）需使用只读接口。
- 备份相关 IPC 对所有角色开放（设置和查看状态），但备份执行在主进程统一进行，不区分角色。

---

## 7. 验收标准

1. 财务经营分析页面正常显示三张图表，支持时间切换、下钻查看明细，数据准确。
2. 老师简报可由财务选择内容生成，预览、导出 PDF 或复制文本，操作流畅；不包含未勾选的敏感信息。
3. 教务可管理多校区和教室，排课时可选择教室，冲突检测正确提示教室冲突。
4. 教室利用率统计正确，能展示空闲时段。
5. 定时备份按设定周期执行，启动时能补备份，备份文件保留数量符合设置，成功/失败有通知。
6. 所有新增功能不影响原有功能，权限隔离依旧有效（教务不可见财务经营分析，助教不可见教室管理写入操作等）。
7. 数据迁移成功，旧数据正常显示，新字段默认值合理。
8. 应用在 Windows 和 macOS 上运行正常。

---

请根据以上需求，在现有代码基础上进行第四次迭代开发。保持技术栈不变（Electron + React + TypeScript + SQLite + Ant Design），代码结构清晰，注释完整。