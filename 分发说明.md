# Vlearn 分发说明（内部使用，面向软件提供方）

## 一、为什么 Windows 安装包要在 Windows 上构建

Vlearn 依赖 better-sqlite3（原生 C++ 模块），该模块必须针对 Windows 工具链编译，**无法在 macOS 上交叉编译**。构建只需做一次，之后可以反复发给任意 Windows 用户使用。

> 用户侧（教务/财务老师）**永远不需要接触任何构建环境**——他们只收到 2 个文件：安装包 exe + 《使用说明书.pdf》。

## 二、构建安装包（两条路线，任选其一）

### 路线 A（推荐·最省事）：任意 Windows 电脑双击一个脚本

无需懂任何命令，只需找一台能联网的 Windows 10/11 电脑（机构前台电脑、同事电脑均可，用完即走）：

1. 把 `vlearn-windows-build-source.zip` 复制到 Windows 电脑，右键 → 全部解压缩；
2. 右键文件夹内的 **`build-windows.bat`** → **以管理员身份运行**（没有该选项就双击）；
3. 脚本自动完成全部工作（自动装 Node.js → 自动装 C++ 编译工具 → 下载依赖 → 打包），全程约 20~40 分钟，中途弹出的窗口一律点"是/允许"；
4. 看到"构建成功"后，安装包在 `release\vlearn-2.0.0-win-x64.exe`（脚本会自动打开该文件夹）。

> 文件夹内的 `BUILD-README.txt` 就是给操作这台电脑的人看的说明，直接连它一起发过去即可。

### 路线 B（无需 Windows 电脑）：GitHub 云端构建

把构建放到 GitHub 的免费云端 Windows 服务器上，浏览器点几下即可，以后每次发新版都复用：

1. **一次性准备**：
   - 在 https://github.com 注册一个账号（免费），创建一个**私有（Private）**仓库；
   - 把本项目文件夹上传到该仓库（`.github/workflows/` 里的云端构建配置已内置在源码包中）；
2. **每次发版**：
   - 浏览器打开仓库页面 → 顶部 **Actions** 标签 → 左侧选"构建安装包（Windows + macOS）" → 右侧 **Run workflow** 按钮 → 确认；
   - 约 15 分钟后刷新页面，进入该次运行 → 底部 **Artifacts** 下载 `vlearn-windows-installer`（Windows 安装包）或 `vlearn-macos-installer`（Mac 安装包）；
   - 注意：Artifacts 保留 30 天，请及时下载。

> 云端服务器直接访问 GitHub 速度快，无需镜像；Mac 安装包也会顺便构建出来。

## 三、发给终端用户什么（发送清单）

每位用户发送 **2 个文件**：

| 文件 | 说明 |
| --- | --- |
| `vlearn-2.0.0-win-x64.exe` | Windows 安装包（按路线 A 或 B 构建） |
| `使用说明书.pdf` | 用户操作指南（已生成，含界面截图） |

外加一段话（可直接复制）：

> 您好，这是 Vlearn 教培管理系统 2.0 安装包和使用说明：
> 1. 双击安装包安装（选个安装位置，一路下一步即可）；旧版用户直接覆盖安装，数据自动迁移，不会丢失；
> 2. 打开软件，先看《使用说明书》第 4 节"快速上手"，10 分钟即可跑通；
> 3. 三个角色登录（教务/财务初始密码 **admin123**，助教初始密码 **assistant123**），请各自登录后在"系统设置"里尽快修改；
> 4. 数据保存在您自己的电脑上，请定期在"系统设置 → 数据备份与恢复"里备份（建议每周一次，存到 U 盘或云盘）。

## 四、用户侧常见问题（供客服参考）

| 问题 | 回答 |
| --- | --- |
| 安装时杀毒软件报风险 | 软件未做数字签名所致，点"仍要运行/允许"即可；如需彻底解决可购买代码签名证书后重新打包 |
| 忘了财务密码 | 联系我方技术人员：备份数据库后删除密码记录，重启软件恢复为 admin123（见 README.md 安全说明） |
| 换电脑了数据怎么办 | 旧电脑"设置 → 备份数据"→ 备份文件拷到新电脑 → "恢复数据" |
| 能两台电脑同时用吗 | 不能联网同步。建议固定一台"主电脑"录入，定期备份 |
| 需要 Mac 版吗 | 路线 B 云端构建会自动产出 dmg；本机也可直接 `npm run dist:mac` |

## 附：路线 A 的脚本内部做了什么（供技术人员排查）

1. 自动申请管理员权限（无则弹窗请求）；
2. `winget` 静默安装 Node.js LTS（如已安装则跳过）与 Visual Studio 2022 生成工具（含"使用 C++ 的桌面开发"工作负载）；
3. 预先解除 PowerShell 脚本执行策略限制；
4. `npm install`（已配国内镜像，prebuild-install 对 GitHub 的超时属预期，会自动转为本地编译）→ `npm run dist:win`；
5. 成功后自动打开 `release` 文件夹。

常见报错排查：

| 现象 | 处理 |
| --- | --- |
| winget 命令不存在 | 微软商店安装"应用安装程序"后重跑脚本 |
| `node-gyp` / `MSBuild` 报错 | VS 生成工具未装完：手动打开"Visual Studio Installer"确认已勾选"使用 C++ 的桌面开发"，然后重跑脚本 |
| `npm.ps1 禁止运行脚本` | 脚本第 3 步已自动处理；如手动构建时遇到，执行 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` 或改用 cmd |
| 下载超时（GitHub 相关） | 国内网络正常现象，脚本已配置 npmmirror 镜像；若仍超时重试一次 |
