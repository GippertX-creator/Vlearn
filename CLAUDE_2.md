# Vlearn 系统更新提示词（v3，基于 v2 已有功能增量开发）

> v2 已实现：三角色物理隔离（教务/财务/助教三库）、角色登录、助教课程记录与微信群短信（外部大模型）、教务/财务 Agent（冲突检测、对账、异常检测、AI 报告等）。
> **v3 更新目标**：
> 1. 优化 Agent 提示词，让生成的课堂总结（微信群短信）语言更优美、更具传播效力；
> 2. 支持同一机构多校区信息同步（人工同步包方式）。

---

## 1. Agent 提示词优化

- 所有面向用户生成文本的系统提示词集中到 `electron/prompts.ts`（SMS_SYSTEM_PROMPT / REPORT_SYSTEM_PROMPT / TREND_SYSTEM_PROMPT / VOUCHER_SYSTEM_PROMPT），便于独立调优。
- **微信群短信**（重点）：
  - 语气亲切自然、简洁有温度，避免公文腔；
  - 课堂表现部分要求**点名表扬具体学生**（家长最愿意转发的内容，即传播效力来源）；
  - 结构清晰（小标题/短段落），适合手机阅读，150~250 字；
  - 结尾行动呼吁 + **下次上课时间预告**（主进程注入该课程下一次课的数据）；
  - 禁止编造、禁止点名批评、禁止营销腔。
- 周报/月报、趋势分析、智能点评同步优化（数据先行、克制专业、建议可执行）。

## 2. 多校区信息同步

### 2.1 需求要点

- 同一机构多个校区（如总部、东城校区）通过**人工同步包**互通信息：导出文件 → 微信/邮件发送 → 对方导入。
- **同步范围**：教务数据（课程/老师/学生/排课/考勤/教务设置）+ 助教数据（课程内容记录）。
- **不同步**：财务库全部数据、登录密码哈希（password_hash）、大模型密钥（ai_api_key）——财务敏感数据与密钥各校区自管。
- 可用角色：教务、助教（菜单"多校区同步"）；财务角色无此菜单。

### 2.2 同步模型与合并规则

- 包 = **全量快照 + 删除墓碑**（每次导出都是完整包 → 导入幂等、顺序无关、丢包自愈）。
- 每张同步表增加三列：`updated_at`（最后修改）、`sync_origin`（来源校区编号）、`sync_remote_id`（来源校区原始 id，重映射后仍可定位原行）。
- 合并规则：
  1. 同来源同一行 → **最近修改优先**（updated_at 大者胜）；
  2. 两个校区各自创建了相同 id 的不同行 → **全部保留**，导入方为新行分配新 id 并**重建全部外键引用**；
  3. 删除墓碑（来源一致且墓碑晚于本地修改）→ 本地级联删除，墓碑继续传播；
  4. 设置表按 key 合并（导入覆盖），password_hash/ai_api_key 过滤。
- 删除埋点：课程/学生/老师删除（含级联的实例/考勤/关联行）、重新生成排课、考勤移除、师生关联变更（仅墓碑被移除的关联）。

### 2.3 校区身份与历史

- `sync.json`（数据目录）：campusId（UUID，自动生成）+ campusName（用户设置，≤30 字）。
- `sync-history.json`：最近 50 条导出/导入记录（时间、来源校区、合并统计），同步页底部展示。

### 2.4 IPC 通道

- `sync:getInfo` / `sync:saveCampusName` / `sync:exportPackage`（保存对话框，文件名 `Vlearn_同步包_<校区>_<日期>.vsync.json`）/ `sync:importPackage`（校验 version=3 后合并，返回统计）。

### 2.5 验收标准

1. 短信内容语言优美、点名表扬、含下次上课预告，未配置大模型时仍正常报错提示；
2. 两个校区双向同步：新增/修改/删除均正确传播；
3. 同 id 冲突数据全部保留（自动重映射），关联关系完整；
4. 密码与 API 密钥不出现在同步包中；财务数据不参与同步；
5. 冒烟测试覆盖：往返同步、LWW、墓碑传播、id 重映射（73 项断言）。
