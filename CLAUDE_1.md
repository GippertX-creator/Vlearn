# Vlearn 系统更新提示词（增量开发，基于第一版已有功能）

> 现有系统已实现：教务管理（课程日历、学生管理、老师管理、考勤）、财务管理（仪表盘、课程费用、学生缴费、老师课酬、盈亏报表）、基础权限（教务与财务分离，财务需密码），技术栈为 Electron + React + TypeScript + SQLite（better-sqlite3），支持 Windows/macOS。  
> **本次更新目标**：  
> 1. 引入三种角色（教务、财务、助教），实现**物理级数据隔离**，三个角色分别使用独立数据库，彼此无法访问对方数据。  
> 2. 新增助教模块，支持课程内容记录、基于外部大模型的微信群短信生成、预留作业批改功能。  
> 3. 新增教务 Agent 与财务 Agent，均接入外部大模型，提供主动提醒、侧边栏建议与报告生成。  
> 4. 保留第一版全部功能，数据自动迁移至新架构。  

---

## 1. 角色与登录系统

### 1.1 三种角色
- **教务老师**：可访问教务数据（课程、学生、老师、考勤），无财务、助教权限。
- **财务老师**：可访问财务数据（缴费、课酬、盈亏）及**只读**访问教务数据（课程、学生、考勤）用于计算，但不可修改教务数据。
- **助教老师**：可访问教务数据（课程日历、学生名单、考勤）及**只读**，用于记录课程内容和生成短信。

### 1.2 登录入口
- 应用启动后显示**角色选择页**，包含三个入口：教务登录、财务登录、助教登录。
- 每个入口均需输入密码，密码独立管理。
  - 初始密码：教务 `admin123`，财务 `admin123`，助教 `assistant123`（可在各自设置中修改）。
- 登录成功后，本地记住登录状态（使用 Electron 的 session 或本地 token 存储），下次启动自动进入该角色主界面，直至手动退出登录（提供“退出登录”按钮）。

### 1.3 物理级数据隔离
- **每个角色使用独立的 SQLite 数据库文件**：
  - `vlearn_academic.db`（教务数据库）
  - `vlearn_finance.db`（财务数据库）
  - `vlearn_assistant.db`（助教数据库）
- 数据库文件存储在用户数据目录（`app.getPath('userData')`）下，通过角色登录决定连接哪个数据库。
- **跨库访问规则**：
  - 财务角色需要读取教务数据（学生、课程、考勤）以计算缴费和课酬，主进程提供**只读 IPC 接口**，从教务数据库查询必要字段，但绝不提供写入或修改接口。
  - 助教角色同样通过只读 IPC 接口读取教务数据（课程日历、学生名单、考勤），但不能访问财务数据库。
  - 任何角色在前端和后端均无法获取其他角色的数据库连接或数据，即使通过修改前端代码或 IPC 调用也无法越权（主进程严格校验当前角色，仅允许访问对应数据库和已授权的只读接口）。

### 1.4 权限矩阵（更新）

| 数据/操作 | 教务 | 财务 | 助教 |
|-----------|------|------|------|
| 课程管理（含排课、调课） | 读/写 | 读 | 读 |
| 学生管理 | 读/写 | 读 | 读 |
| 老师管理 | 读/写 | 读 | 读 |
| 考勤管理 | 读/写 | 读 | 读 |
| 课程费用 | 不可见 | 读/写 | 不可见 |
| 学生缴费 | 不可见 | 读/写 | 不可见 |
| 老师课酬 | 不可见 | 读/写 | 不可见 |
| 盈亏报表 | 不可见 | 读 | 不可见 |
| 助教课程内容记录 | 不可见 | 不可见 | 读/写 |
| 助教短信生成 | 不可见 | 不可见 | 使用 |

---

## 2. 数据库架构调整

### 2.1 教务数据库（vlearn_academic.db）
保留原教务相关表：`courses`、`teachers`、`teacher_courses`、`students`、`student_courses`、`schedule_instances`、`attendances`。  
**新增表**：
- `settings`（键值对，存储教务密码哈希、Agent 配置等）

### 2.2 财务数据库（vlearn_finance.db）
保留原财务相关表：`student_payments`、`teacher_payments`、`settings`。  
**新增表**：
- `settings`（存储财务密码哈希、Agent 配置等）

### 2.3 助教数据库（vlearn_assistant.db）
新建数据库，包含以下表：

```sql
-- 助教密码与配置
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT
);

-- 课程内容记录（每次课后填写）
CREATE TABLE lesson_notes (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  schedule_instance_id INTEGER NOT NULL,   -- 关联教务库的课程实例ID（通过只读接口获取）
  knowledge_points TEXT,                   -- 知识点（结构化，逗号分隔或JSON数组）
  class_performance TEXT,                  -- 课堂表现（文本）
  homework TEXT,                           -- 当日作业（文本）
  summary TEXT,                            -- 当日总结（文本）
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
);

-- 生成的微信群短信缓存
CREATE TABLE generated_messages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  lesson_note_id INTEGER NOT NULL,
  message_content TEXT NOT NULL,
  generated_at TEXT DEFAULT (datetime('now')),
  FOREIGN KEY (lesson_note_id) REFERENCES lesson_notes(id) ON DELETE CASCADE
);

-- 预留作业批改字段（未开发）
ALTER TABLE lesson_notes ADD COLUMN homework_grading TEXT DEFAULT '未开发，敬请期待';
```

### 2.4 数据迁移
- 首次启动更新版本时，自动检测旧版单数据库（假设为 `vlearn.db`），将原表分别迁移至三个新数据库：
  - 教务相关表 → `vlearn_academic.db`
  - 财务相关表 → `vlearn_finance.db`
  - 原 `settings` 中的年级、缴费方式等通用配置，分别复制到教务和财务的 `settings` 表（若需要）。
- 迁移完成后删除旧数据库文件（或重命名为备份）。
- 提供迁移日志，若失败则回滚并提示用户。

---

## 3. 助教模块详细需求

### 3.1 功能概述
助教登录后，主界面显示**课程日历**（与教务日历类似，只读）。助教可查看所有课程实例及学生名单、考勤状态。点击某个课程实例，进入“课程内容记录”页面，填写本次课的总结和作业，并可一键生成微信群短信。

### 3.2 课程内容记录
- 页面包含以下字段（均为可填写项，可留空）：
  - **知识点**：以标签形式添加，支持输入后回车添加多个，也可自由文本输入（存储为逗号分隔字符串）。
  - **课堂表现**：多行文本框，自由描述。
  - **当日作业**：多行文本框，自由描述。
  - **当日总结**：多行文本框，自由描述。
- 保存后生成 `lesson_notes` 记录，关联对应的 `schedule_instance_id`（从教务数据库读取的课程实例ID，通过 IPC 获取）。

### 3.3 微信群短信生成
- 在课程内容记录页面或课程实例详情弹窗中，提供“生成微信群短信”按钮。
- 点击后，调用**外部大模型 API**，将当前填写的知识点、课堂表现、作业、总结等数据组织成一段适合发送到微信群的文字，格式可参考：
  > 【课程通知】  
  > 今日数学A1班课程总结：  
  > 知识点：函数单调性、导数应用  
  > 课堂表现：整体良好，张三回答问题积极  
  > 作业：完成练习册P45-46  
  > 请家长督促完成，谢谢！
- 生成的短信内容显示在弹窗中，提供“复制”按钮，用户可手动粘贴到微信群。
- **大模型 API 配置**：在助教设置页面中，用户需填写 API URL 和 API Key（必填），保存后生效。所有参数（如模型名称、温度、提示词等）由开发者预先写死，用户不可修改。
- 生成结果自动保存到 `generated_messages` 表，便于历史查看。
- 若 API 调用失败，显示错误信息，不保存。

### 3.4 预留作业批改功能
- 在课程内容记录页面底部显示一个置灰按钮“作业批改（未开发）”，点击提示“该功能即将上线，敬请期待”。
- 数据库已预留 `homework_grading` 字段，本次不实现任何批改逻辑。

---

## 4. 教务 Agent 详细需求

### 4.1 功能清单
1. **排课冲突检测**：当教务新增课程或调整课程实例时，自动检测老师、学生时间冲突（若一个学生同时报了两门时间重叠的课程，或老师同时有两个课程实例），并在保存前弹出提醒。
2. **智能调课建议**：当教务尝试调整某个课程实例时，若发生冲突，Agent 根据老师与学生的空闲时间（从现有排课数据推算）推荐 2-3 个可行的替补时间段，供教务选择。
3. **考勤异常提醒**：每日首次登录或定时（每 4 小时）检查考勤数据，若某学生连续 3 次缺勤或请假，自动在侧边栏显示提醒卡片，点击可查看详情。
4. **学生/老师信息补全提示**：在新增学生或老师时，若姓名与已有记录高度相似（如“张三” vs “张 三”），提示可能重复；若关键字段（如学校班级、所报课程）为空，提示完善。
5. **自动生成周报/月报**：教务可在“报告”页面点击“生成周报”或“生成月报”，Agent 汇总出勤率、课程进度等数据，生成文本报告，并支持导出为 PDF 或 Word（可先支持纯文本复制）。

### 4.2 实现方式
- 教务 Agent 同样依赖外部大模型 API，与助教共用一套配置（在教务设置中填写 API URL 和 Key），参数由开发者针对不同功能预设。
- **触发机制**：
  - 冲突检测和调课建议在用户操作过程中实时触发，显示为弹窗或侧边栏建议。
  - 考勤异常提醒和补全提示为主动弹出（侧边栏通知），频率可配置。
  - 周报/月报由用户点击按钮触发。
- **Agent 配置**：在教务设置中提供开关（启用/禁用每个功能）和“灵敏度”调节（例如冲突检测的时间容忍度、考勤异常连续次数阈值）。

### 4.3 输出形式
- 所有提醒和建议以**侧边栏**形式展示（应用右侧固定面板），可收起/展开。
- 报告生成后在弹窗中显示，提供复制和导出按钮。

---

## 5. 财务 Agent 详细需求

### 5.1 功能清单
1. **自动对账提醒**（功能1）：**需用户主动点击“对账检查”按钮**触发，不可自动弹出。检查学生应缴与实缴是否一致、老师应付与实付是否一致，列出所有差异项，并给出可能原因。
2. **缴费逾期预警**：每日首次登录或定时检查，预测未来 7 天内可能欠费的学生（根据缴费记录和课程周期），主动弹出侧边栏提醒。
3. **盈亏趋势分析**：在盈亏报表页面提供“AI 趋势分析”按钮，点击后生成未来 3 个月的盈亏预测文字报告。
4. **自动生成凭证/报表**：在财务各列表页面提供“生成凭证”或“导出智能报表”按钮，Agent 根据当前筛选结果生成标准格式的财务报表（文本），可复制。
5. **异常交易检测**：在每次新增或修改缴费/支付记录时，自动检测异常（如单笔金额超过平均值 3 倍、频繁修改同一记录），弹出警告提示。

### 5.2 实现方式
- 财务 Agent 使用与助教、教务共用的 API 配置（在财务设置中填写 URL 和 Key），但参数不同，由开发者预设。
- **触发机制**：
  - 功能1（自动对账提醒）仅通过按钮触发。
  - 功能2（缴费逾期预警）和功能5（异常交易检测）为主动弹出提醒（侧边栏通知）。
  - 功能3、4 由用户点击按钮触发。
- **Agent 配置**：在财务设置中提供开关和灵敏度调节（如逾期预警提前天数、异常检测阈值倍数）。

### 5.3 权限与数据访问
- 财务 Agent 可读取财务数据库全部数据，并通过只读 IPC 接口读取教务数据库中的学生、课程、考勤数据，用于计算应缴和判断欠费。
- 财务 Agent 绝不能修改教务数据。

---

## 6. 界面与交互更新

### 6.1 角色选择页
- 居中显示三个大按钮：“教务登录”、“财务登录”、“助教登录”，下方有版本号。
- 点击后弹出密码输入框，验证通过后进入对应主界面。

### 6.2 主界面框架
- 统一使用 Ant Design 布局：顶部导航栏（角色标识、菜单、退出登录）、左侧菜单（各模块页面）、右侧内容区。
- 侧边栏（Agent 通知）固定在内容区右侧，宽度约 300px，可折叠，默认展开。
- 三个角色的主界面风格一致，但菜单项不同。

### 6.3 各角色菜单
- **教务**：课程日历、学生管理、老师管理、报告中心（周报/月报）、系统设置（含 Agent 配置、密码修改）。
- **财务**：仪表盘、课程费用、学生缴费、老师课酬、盈亏报表、系统设置（含 Agent 配置、密码修改）。
- **助教**：课程日历、课程内容记录、历史消息、系统设置（含 API 配置、密码修改）。

### 6.4 通用组件
- 侧边栏通知组件：用于显示 Agent 提醒和建议，支持不同类型（警告、信息、建议）、可关闭、可查看详情。
- Agent 配置组件：每个角色设置页中均有 API URL 和 API Key 输入框（助教为必填，教务/财务可留空表示不启用 Agent），以及各功能的开关和灵敏度滑块。

---

## 7. 数据迁移与兼容性

- 自动检测旧数据库文件，执行迁移脚本，将原表和数据分别导入三个新数据库。
- 迁移过程中保留原有密码（教务/财务的密码哈希）到相应数据库的 `settings` 表。
- 若迁移失败，弹窗提示并提供回滚选项（保留旧数据库备份）。
- 所有新功能不影响第一版已有功能的正常使用。

---

## 8. 验收标准

1. 三个角色登录入口独立，密码正确性验证，登录状态保持。
2. 任何角色无法通过界面、IPC 调用或修改本地文件访问其他角色的数据库（使用独立数据库文件，并验证跨库访问被拒绝）。
3. 助教可查看课程日历、填写课程内容、生成微信短信并复制；短信内容合理且基于外部大模型生成；API 配置生效。
4. 教务 Agent 的冲突检测、调课建议、考勤提醒、补全提示、报告生成均按预期工作，侧边栏提醒及时准确。
5. 财务 Agent 的对账检查（按钮触发）、逾期预警、趋势分析、报表生成、异常检测均按预期工作。
6. 所有 Agent 功能均可通过设置开关和灵敏度调节，关闭后不再触发。
7. 数据迁移成功，旧数据完整保留。
8. 应用在 Windows 和 macOS 上均可正常安装、运行。

---

请基于以上更新需求，在现有代码基础上进行增量开发，确保代码结构清晰、注释完整，并保持与第一版技术栈一致。